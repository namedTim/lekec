import 'package:flutter/material.dart';

class MedsHistoryScreen extends StatelessWidget {
  const MedsHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Zgodovina'),
      ),
    );
  }
}
