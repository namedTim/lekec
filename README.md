# lekec

A flutter project.

## Database

use:  dart run build_runner build --delete-conflicting-outputs
it generates the newly added database classes

# App build
to build production 
flutter run --release

# todos
https://pub.dev/packages/flutter_launcher_icons
https://developer.android.com/studio/write/create-app-icons

